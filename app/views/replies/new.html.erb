<h1>Reply to <%= @post.title %></h1>

<%= form_for [@post, @reply] do |f| %>
  <p>
  <%= f.label :content %><br>
  <%= f.text_area :content %>
  </p>

  <p>
  <%= f.submit "Comment" %>
  </p>
<% end %>

